{
  "name": "behavior-modeling-course",
  "version": "1.0.0",
  "private": true,
  "description": "Unified repository for Introduction to Mathematical Modeling in Behavior Science — handbook manuscript and companion website sharing canonical content.",
  "scripts": {
    "dev": "cd website && npm run dev",
    "build:website": "cd website && npm run build",
    "build:leanpub": "npx tsx scripts/build-leanpub.ts",
    "validate": "npx tsx scripts/validate-metadata.ts",
    "build:all": "npm run validate && npm run build:leanpub && npm run build:website",
    "clean": "rm -rf website/.next"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "gray-matter": "^4.0.3"
  }
}
